import * as C from "@utils/constants";

const NAV_MENU_PATHS = Object.freeze({
  HOME_PAGE: C.HOME_ROUTE,
  ABOUT_PAGE: C.ABOUT_ROUTE,
  CONTACT_PAGE: C.CONTANCT_ROUTE,
  GALLERY_PAGE: C.GALLERY_ROUTE,
  PAGE_NOT_FOUND: "*",
});

export const MENU = {
  NAV_MENU_PATHS,
};
